# Set default behavior, in case users don't have core.autocrlf set.
* text=auto

# Explicitly declare text files you want to always be normalized to LF in the repo
*.qmd text eol=lf
*.md text eol=lf
*.html text eol=lf
*.css text eol=lf
*.js text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.json text eol=lf
*.py text eol=lf
*.R text eol=lf
*.sh text eol=lf # Ensure shell scripts have LF

# Declare files that will always have CRLF line endings (e.g. Windows batch files)
# *.bat text eol=crlf

# Declare files that should not be touched (binary files)
*.png binary
*.jpg binary
*.pdf binary